User-agent: *
Allow: /

# /decks/ is client collateral. It is deliberately NOT disallowed here.
# Disallow blocks crawling but does not prevent indexing, and it publicly
# advertises the directory. The X-Robots-Tag noindex header in netlify.toml
# does the actual job, and the crawler has to be allowed in to read it.

Sitemap: https://syntropyearth.com/sitemap.xml
